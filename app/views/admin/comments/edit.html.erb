
Edit Comment:
<%= form_for [:admin, @post, @comment] do |f|%>
    <%= f.text_area :body, autofocus: true, autocomplete: "body"%>
    <%= f.text_area :user_id, value: current_user.id, class:'d-none'%>
    <i><%= f.submit "save", type: "submit", :class=> "btn btn-primary" %></i>
<%end%>